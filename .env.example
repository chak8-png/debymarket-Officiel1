# Base de données — OPTIONNEL en dev.
# Sans DATABASE_URL, le site tourne en "mode démo" (données de démonstration en mémoire).
DATABASE_URL=

# Admin — ⚠️ valeur de dev uniquement, mot de passe FORT obligatoire en production
ADMIN_PASSWORD=Debymarket2026

# App
NEXT_PUBLIC_APP_URL=http://localhost:3000

# (Optionnel) Statistiques Hotjar — votre Site ID (chiffres). Laisser vide = désactivé.
NEXT_PUBLIC_HOTJAR_ID=
